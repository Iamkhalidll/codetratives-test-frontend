{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install --save-dev typescript @types/react @types/node --legacy-peer-deps",
        "buildCommand": "next build"
      }
    }
  ]
}